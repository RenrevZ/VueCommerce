<template>
    <div class="range-btn">

        <div class="minus-btn" @click="decrementValue">
            <img src="../assets/icon-minus.svg" 
                alt="">
        </div>

        <div class="item-value">
            {{ itemValue }}
        </div>

        <div class="plus-btn" 
            @click="incrementValue">
            <img src="../assets/icon-plus.svg" 
                alt="">
        </div>

    </div>
</template>
<script setup>
    import { defineProps,defineEmits } from 'vue'

    defineProps({
        'itemValue':Number
    })

    const emit = defineEmits(['decrementValue','incrementValue'])

    const decrementValue = () => {
        emit('decrementValue')
    }

    const incrementValue = () => {
        emit('incrementValue')
    }
</script>

<style>
    .range-btn{
        width: 100%;
        height: 100%;
        background-color: var(--Light-grayish-blue);
        display: grid;
        grid-template-columns: 5px 15px 5px;
        gap:10px;
        align-items: center;
        justify-content: space-around;
        user-select: none;
        border-radius: 10px;
    }

    .range-btn .minus-btn{
        cursor: pointer;
        position: relative;
        top: -3px;
    }

    .range-btn .minus-btn:active,
    .range-btn .plus-btn:active{
       scale: 0.8;
    }
 
   
    .range-btn .item-value{
        padding: 0px 10px;
    }

    .range-btn .plus-btn{
        cursor: pointer;
    }
</style>